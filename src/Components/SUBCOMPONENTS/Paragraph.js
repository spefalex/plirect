

















































































































































































































































































































































































































































































































































import React from 'react';

export default class Paragraph extends React.Component{
  render(){
    return(
      <p className={this.props.classP}>{this.props.paraphCtn}</p>
    )
  }

}

Paragraph.defaultProps = {
  classP : ''
};
